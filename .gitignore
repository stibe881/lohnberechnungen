# Zugangsdaten für die Claude-Weiterleitung
api/config.php

# Interne Einstellungsdateien (Vorlagen, Gehaltstabellen) nie einchecken
*einstellungen*.json
*settings*.json

.DS_Store

# Zentrale Einstellungen auf dem Server (werden von api/settings.php geschrieben)
api/data/*
!api/data/.htaccess
